# Cap Table frontend environment.

# Hedera testnet RPC (chain id 296).
VITE_HEDERA_RPC=https://testnet.hashio.io/api
VITE_HEDERA_RPC_FALLBACK=https://testnet.arkhia.io/api

# Indexer REST API. Must be set for a production build (the build fails otherwise).
VITE_INDEXER_API=http://localhost:8080
